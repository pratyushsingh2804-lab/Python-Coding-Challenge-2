# Program to remove duplicates from a list

numbers = [1, 1, 2, 3]

result = []  # Store unique elements

# Loop through list
for num in numbers:
    if num not in result:
        result.append(num)  # Add only unique

# Print result
print("List after removing duplicates:", result)
