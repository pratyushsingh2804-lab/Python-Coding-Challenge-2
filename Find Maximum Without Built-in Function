# Program to find maximum value without using max()

numbers = [5, 8, 2]

# Assume first element is maximum
max_num = numbers[0]

# Loop through list
for num in numbers:
    if num > max_num:
        max_num = num  # Update maximum

# Print result
print("Maximum value =", max_num)
