# Function to return even numbers from list

def get_even(numbers):
    result = []
    for num in numbers:
        if num % 2 == 0:
            result.append(num)
    return result

# Example usage
print(get_even([1, 2, 3, 4]))
