# Function to check if number is prime

def is_prime(num):
    if num <= 1:
        return "Not Prime"
    
    # Check divisibility from 2 to num-1
    for i in range(2, num):
        if num % i == 0:
            return "Not Prime"
    
    return "Prime"

# Example usage
print(is_prime(7))
